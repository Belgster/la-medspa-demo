---
type Variant = 'primary' | 'secondary' | 'ghost' | 'on-dark';
type Size = 'sm' | 'md' | 'lg';

interface Props {
  href: string;
  variant?: Variant;
  size?: Size;
  external?: boolean;
  class?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  external = false,
  class: className = '',
} = Astro.props;

const base =
  'inline-flex items-center gap-[10px] font-display font-medium uppercase tracking-[0.12em] ' +
  'rounded-pill border border-transparent whitespace-nowrap cursor-pointer no-underline ' +
  'transition-all duration-la-base ease-la-ease ' +
  'focus-visible:outline-none focus-visible:shadow-focus';

const sizes: Record<Size, string> = {
  sm: 'text-[11px] px-[20px] py-[10px]',
  md: 'text-[13px] px-[28px] py-[14px]',
  lg: 'text-[14px] px-[36px] py-[18px]',
};

const variants: Record<Variant, string> = {
  primary:
    'bg-gold text-ink border-gold ' +
    'hover:bg-transparent hover:text-brown hover:border-gold ' +
    'active:bg-gold-hover active:border-gold-hover',
  secondary: 'bg-transparent text-brown border-brown hover:bg-brown hover:text-cream',
  ghost:
    'bg-transparent text-brown border-transparent px-[14px] ' +
    'hover:bg-gold-wash hover:text-ink',
  // Sits on photographic backgrounds where image luminance varies. The
  // semi-transparent dark fill + backdrop blur guarantees AA text contrast
  // independent of the underlying image — the previous transparent + cream
  // border failed on the lighter zones of the hero image.
  'on-dark':
    'bg-ink/50 backdrop-blur-sm text-cream border-cream/40 ' +
    'hover:bg-gold hover:text-ink hover:border-gold',
};
---
<a
  href={href}
  class={`${base} ${sizes[size]} ${variants[variant]} ${className}`}
  target={external ? '_blank' : undefined}
  rel={external ? 'noopener noreferrer' : undefined}
>
  <slot />
</a>
